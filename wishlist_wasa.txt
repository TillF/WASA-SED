Wishlist WASA-SED - proposed features and improvements
------------------------------------------------------
Please add the proposer's name in brackets - in case clarification is needed. 

responsibility all:
- improve performance indicated with "ii" in the source code
- abschaltbare Ausgabedateien (siehe outfiles.dat)(optimierte Geschwindigkeit) (Till)
- Tab separated output in all output files (see output of hillslope routines)(Till)
- flexible input-files: Reihenfolge variierbar  und durch Schlüsselworte gekennzeichnet(Till)
	und/oder(Till)
- control-files getrennt nach Aufgabenfeld (Hill,river, reservoir;hydro, ero)(Till)
- dynamischer/wählbarer Zeitraum, in dem Ergebniss gespeichert werden bis zu Ausgabe in Dateien(Till)
- Versionsnummer in Kopfzeile der Ein-/Ausgabegabedateien(Till)
- im Quelltext Begriff SOTER durch LU, Mun(icipality) durch Basin ersetzen(Till)
- globally switch off/on debugging screen output(Till)
- option "append to existing output files" if a run is recommenced(Till)
- remove water_subbasin, sediment_subbasin, replace by *._t(Till)
- vectorize array assignments, use implicit loops for file output (speed!)(Till)
- also use hourly humidity,temperature,radiation; not only rainfall(Till)
- allow output in hourly resolution for also for other variables than water_subbasin(Till)
- graceful termination if input files are not found

responsibility Till:
- create output directory, if non-existent(Till)
- open input files with read-only (allows access to opened files, done for climo-files)(Till)
- read maxsoter, maxterrain, maxsoil, maxhori, ntrans from structure of inputfile rather than from maxdim, nveg, nsoil(Till)
- multiple Subbasins could use the same climate series(Till)
- Tolerante Reihenfolge der Ids in den unterschiedlichen Eingabedateien (realisiert für soil_particles.dat, rainy_seasons.dat,climo-files (except rain_hourly))(Till)
- Fließzeiten innerhalb TC beachten, variable Verknüpfung der SCVs untereinander(Till)
- Index für Klumpung der SVCs innerhalb einer TC(Till)
- TC mit variabler Breite, um konzentrierten Abfluss erzeugen zu können(Till)
– Nbrsoil enthält die Anzahl der Böden in einer TC. Dies muss eigentlich nicht für jede subbasin-LU-TC-Kombination extra gespeichert werden, sondern nur einmal für den TC-Typ. Ebenso bei Rocky (enthält Anteil an Felsen in einer TC). Ebenso ID_soil_intern. Ebenso horiz_thickness, svcrooth, svcbedr. Bei Speicherbedarf, umbauen, Außer  horiz_thickness, die sich ggf. durch Erosion verringern könnte(Till)
- Einbauen: keine Vegetation(Till)
- Unterschied gwsu / deepgwrsu klären --> ggf redundanz deepgw_r, gw_recharge aufheben(Till)
- revise system of input files: eliminate redundancies, strict relational database scheme(Till)


